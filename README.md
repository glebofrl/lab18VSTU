# **База данных железнодорожных поездов**

Консольное приложение для создания, хранения и управления информацией о железнодорожных поездах.

---

## 🎯 **Назначение**

Проект демонстрирует практическое применение структур данных, файлового ввода-вывода и алгоритмов поиска/сортировки в языке C. Приложение позволяет управлять базой данных поездов с возможностью автоматического заполнения, ручного ввода, поиска по направлениям и сортировки данных.

**Образовательные цели:**
- Работа со сложными структурами данных в C
- Реализация CRUD-операций с динамической памятью
- Использование стандартной библиотеки для сортировки
- Обработка текстовых и бинарных файлов
- Создание консольного интерфейса с меню

---

## 📋 **Функциональные возможности**

### **Основной функционал:**

- ✅ **Создание записей:** Ручной или автоматический ввод данных
- ✅ **Чтение данных:** Просмотр всей базы в табличном формате
- ✅ **Поиск:** По направлению движения поезда
- ✅ **Сортировка:** По расстоянию (возрастание)
- ✅ **Сохранение:** Текстовый формат для долговременного хранения
- ✅ **Консольный интерфейс:** Интерактивное меню с 4 основными командами

### **Структура данных поезда:**

| Поле | Тип | Описание |
|------|-----|----------|
| `num_of_train` | `int` | Номер поезда |
| `arrival_time` | `int` | Время прибытия в минутах (0-1439) |
| `departure_time` | `int` | Время отбытия в минутах (0-1439) |
| `direction` | `char[256]` | Направление движения |
| `distance` | `int` | Расстояние маршрута в километрах |

### **Дополнительные вычисляемые поля:**
- `speed` - Средняя скорость поезда (км/ч), рассчитывается автоматически

---

## 🏗️ **Архитектура проекта**

### **Ключевые алгоритмы:**

1. **Расчет скорости:** Функция `train_speed()` вычисляет среднюю скорость с учетом циклического времени
2. **Поиск:** Линейный поиск по полю `direction` с использованием `strcmp()`
3. **Сортировка:** Быстрая сортировка через `qsort()` по полю `distance`
4. **Генерация данных:** Автоматическое заполнение случайными значениями с проверкой корректности

### **Основные функции:**

| Функция | Назначение |
|---------|------------|
| `fillTrainArray()` | Автоматическое заполнение массива случайными данными |
| `fill_array()` | Ручной ввод данных с клавиатуры |
| `print_array()` | Форматированный вывод таблицы в консоль |
| `search_trains()` | Поиск поезда по направлению |
| `compare()` | Функция сравнения для сортировки |
| `output_file()` | Сохранение данных в текстовый файл |
| `input_file()` | Загрузка данных из файла |

---

## 🚀 **Сборка и запуск**

### **Требования:**

- Компилятор C (GCC/Clang/MSVC)
- Стандарт C99 или новее
- Операционная система: Windows/Linux/macOS

### **Инструкция по сборке:**

```bash
# 1. Сохраните код в файл train_database.c

# 2. Компиляция с включением необходимых библиотек
gcc -std=c99 -o train_database train_database.c

# 3. Запуск программы
./train_database    # Linux/macOS
train_database.exe  # Windows
```

## 🚀 Использование

1. **Запустите программу**
2. **Введите количество записей** для создания базы данных
3. **Выберите способ заполнения:**
   - `1` - Автоматически (случайные данные)
   - `2` - Вручную с клавиатуры
4. **Выберите действие:**
   - `1` - Поиск поезда по направлению
   - `2` - Сортировка по расстоянию
   - `3` - Сохранение в файл
   - `4` - Загрузка из файла
5. **Следуйте инструкциям в консоли**
6. **Для выхода** нажмите Enter после выполнения операции

## 📁 Формат файлов

**Выходные файлы:** Текстовый формат с разделителями

**Пример записи:**
Номер: 1 ; Время прибытия: 08:30 ; Время отбытия: 07:15 ; Направление: Москва-СПб ; Расстояние (км): 300 ; Средняя скорость (км/ч): 156.25

## 📊 Пример работы программы

```text
Введите количество записей: 3
Выберите действие:
1. Заполнить массив случайными числами
2. Заполнить массив с клавиатуры
1

|  N  || Время прибытия || Время отбытия ||         Направление         || Расстояние (км) || Средняя скорость (км/ч) |
_____________________________________________________________________________________________________________________
|  1  ||     08:30      ||     07:15     ||         Москва-СПб         ||        300     ||          156.25          |
|  2  ||     14:20      ||     12:45     ||         СПб-Москва         ||        450     ||          180.00          |
|  3  ||     21:10      ||     19:30     ||      Москва-Казань        ||        280     ||          140.00          |

Выберите действие:
1. Найти элемент массива по направлению
2. Отсортировать массив
3. Выгрузить массив в файл
4. Загрузить массив из файла
1

Введите направление: Москва-СПб
Найден поезд 1
Направление: Москва-СПб
```
## 🔧 Технические особенности

### Обработка времени:
```c
// Хранение времени в минутах от начала суток
typedef struct {
    int departure_minutes;  // Время отбытия (0-1439)
    int arrival_minutes;    // Время прибытия (0-1439)
} TrainTime;

// Конвертация строки ЧЧ:ММ в минуты
int time_to_minutes(const char* time_str) {
    int hours, minutes;
    sscanf(time_str, "%d:%d", &hours, &minutes);
    return hours * 60 + minutes;
}

// Конвертация минут в строку ЧЧ:ММ
void minutes_to_time(int minutes, char* buffer) {
    int hours = minutes / 60;
    int mins = minutes % 60;
    sprintf(buffer, "%02d:%02d", hours, mins);
}

// Расчет скорости с учетом перехода через полночь
double calculate_speed(int distance, int departure, int arrival) {
    int travel_time;
    if (arrival >= departure) {
        travel_time = arrival - departure;
    } else {
        travel_time = (1440 - departure) + arrival;
    }
    return (travel_time > 0) ? (distance / (travel_time / 60.0)) : 0;
}
```
##🔧 Технические особенности
1. Обработка времени:
- Время хранится в минутах от начала суток (0-1439)
- Автоматический расчет скорости учитывает переход через полночь
- Форматированный вывод в читаемом формате ЧЧ:ММ

2. Управление памятью:
- Динамическое выделение памяти под массив структур
- Корректное освобождение ресурсов
- Проверка границ массивов

3. Обработка ошибок:
- Проверка корректности ввода пользователя
- Обработка ошибок открытия/записи файлов
- Валидация временных интервалов

## 🤝 Вклад в проект
Приветствуются улучшения и расширения функционала.

1. Планируемые улучшения:
- Добавление бинарного формата хранения
- Реализация удаления и редактирования записей
- Поиск по диапазонам (время, расстояние)
- Статистика: самые быстрые/длинные маршруты


## Автор
Автор: Глебов Илья, бИПТ-252
Курс: 1
Год: 2025
Лицензия: MIT

Проект создан в образовательных целях

